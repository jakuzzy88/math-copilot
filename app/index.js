/**
 * React Native application entry point.
 *
 * Sprint 6B: React Native Camera Shell.
 */
import { AppRegistry } from 'react-native';
import App from './src/App';

AppRegistry.registerComponent('MathCopilot', () => App);
